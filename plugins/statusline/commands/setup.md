---
description: Install and configure the statusline for Claude Code
---

## Context

Current platform: !`echo $OSTYPE`
Claude config directory location: !`echo $HOME/.claude`

## Your task

Install the statusline by running the installation script. Execute this command:

```bash
node plugins/statusline/scripts/install-statusline.mjs
```

After installation completes, inform the user that they need to restart Claude Code for the statusline to appear.
